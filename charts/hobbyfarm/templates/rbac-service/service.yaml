apiVersion: v1
kind: Service
metadata:
  name: rbac-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    component: rbac-service
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.rbac.apiPort }}
  - name: grpc
    protocol: TCP
    port: {{ .Values.rbac.grpcPort }}
