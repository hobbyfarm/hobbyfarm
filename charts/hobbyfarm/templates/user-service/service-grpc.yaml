apiVersion: v1
kind: Service
metadata:
  name: user-service-grpc
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  selector:
    component: user-service
  ports:
  - name: grpc
    protocol: TCP
    port: 8080
